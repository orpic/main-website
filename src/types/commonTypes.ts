export interface navigationTabItem {
  name: string;
  activeIcon: string;
  inActiveIcon: string;
}

export interface FeaturedSectionCardProps {
  src: string;
  description: string;
  link: string;
  linkText: string;
}
